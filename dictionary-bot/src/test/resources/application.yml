spring:
  profiles:
    active: test

---

#spring:
#  config:
#    activate:
#      on-profile: test

---
spring:
  profiles:
    group:
      "test": "default,window,dev"
      "test_jpa": "default,window,dev"