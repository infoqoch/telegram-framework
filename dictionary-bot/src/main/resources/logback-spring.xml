<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_PREFIX" value="dictionary-bot"></property>
    <springProfile name="os-window">
        <property name="LOG_PATH" value="c:/logs/dictionary-bot"/>
    </springProfile>
    <springProfile name="os-linux">
        <property name="LOG_PATH" value="./log"/>
    </springProfile>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="infoqoch.dictionarybot.system.log.IgnoreLogFilter"/>
        <file>${LOG_PATH}/${LOG_PREFIX}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_PREFIX}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="infoqoch.dictionarybot.system.log.IgnoreLogFilter"/>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>


    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <logger name="infoqoch.dictionarybot" level="DEBUG" additivity="true"/> <!--프로젝트 로그-->
    <logger name="org.hibernate.SQL" level="INFO" additivity="true" /> <!--sql 로그-->
    <!--<logger name="org.hibernate.type.descriptor.sql" level="TRACE" additivity="true" />--> <!--파라미터 로그-->
</configuration>
